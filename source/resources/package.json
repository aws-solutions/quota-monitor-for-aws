{
  "name": "quota-monitor",
  "version": "6.2.2",
  "description": "cdk resources to provision needed infrastructure",
  "author": {
    "name": "Amazon Web Services",
    "url": "https://aws.amazon.com/solutions"
  },
  "license": "Apache-2.0",
  "scripts": {
    "cleanup": "rm -rf node_modules && rm -f package-lock.json",
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "pretest": "npm ci",
    "test": "npx jest",
    "cdk": "npx cdk",
    "orgHub:deploy": "echo 'fixing stackset template paths for cdk assets by forcing it to synth twice' && rm -rf cdk.out && cdk synth && cdk deploy"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^18.17.2",
    "aws-cdk": "^2.84.0",
    "aws-cdk-lib": "^2.101.0",
    "@aws-cdk/aws-servicecatalogappregistry-alpha": "^2.101.0-alpha.0",
    "cdk-nag": "^2.14.39",
    "constructs": "^10.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.7.0",
    "typescript": "5.2.2"
  }
}
